@layout('main')
@section('content')
    <div class='jumbotron'>
        <h1>{{ task.title}}</h1>
            <p>{{task.body}}</p>
                <hr class="my-4">
                    <a href="/tasks" class="btn btn-primary btn-kg">Retornar</a>
    <a href="/remove/{{ task.id }}" class="btn btn-danger btn-kg">Remover Tarefa</a>


    </div> 
@endsection